apiVersion: 1

providers:
  - name: LedgerBridge
    orgId: 1
    folder: LedgerBridge
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
