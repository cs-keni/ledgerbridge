# ── Demo profile ─────────────────────────────────────────────────────────────────
# Activated via SPRING_PROFILES_ACTIVE=demo (Railway deploy, Phase 7.5)
# Adds profile-gated Flyway migration location for demo seed data (decision D5)

spring.flyway.locations=classpath:db/migration,classpath:db/demo

# Logging: keep INFO in demo so Railway log viewer is readable
logging.level.com.ledgerbridge=INFO
